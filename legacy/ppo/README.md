# legacy/ppo

PPO / self-play era (M1-M3), dormant since the M4 reframe (docs/FINDINGS.md "M4 reframed").
Kept runnable: `uv run python legacy/ppo/train.py ...` (each file inserts the repo root on sys.path).
